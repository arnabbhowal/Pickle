import 'package:flutter/material.dart';

class ChooseForMePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Choose for Me')),
      body: Center(
        child: Text('Random Selection Content'),
      ),
    );
  }
} 