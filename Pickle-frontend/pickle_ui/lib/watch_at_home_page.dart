import 'package:flutter/material.dart';

class WatchAtHomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Watch at Home')),
      body: Center(
        child: Text('Watch at Home Content'),
      ),
    );
  }
} 