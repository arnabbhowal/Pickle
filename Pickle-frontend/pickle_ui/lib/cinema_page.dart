import 'package:flutter/material.dart';

class CinemaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Nearby Cinemas')),
      body: Center(
        child: Text('Cinema Search Content'),
      ),
    );
  }
} 